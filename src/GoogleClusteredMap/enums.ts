export enum MapTypeId {
  HYBRID = "hybrid",
  ROADMAP = "roadmap",
  SATELLITE = "satellite",
  TERRAIN = "terrain",
}

export enum MarkerType {
  CLUSTER = "cluster",
  ITEM = "item",
  USER_LOCATION = "user-location",
}
